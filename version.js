// Единая точка версии — читается и приложением (index.html), и service worker'ом (sw.js через importScripts).
// При каждой публикации новой версии для коллег меняйте это число — тогда у всех обновится кэш и покажется плашка "Доступно обновление".
const APP_VERSION = '1.13.0';
